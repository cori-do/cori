{
  "name": "@cori-do/runner",
  "version": "0.2.3",
  "description": "Deno-based runner that hosts Cori `code` activities in a sandboxed subprocess.",
  "license": "MIT",
  "private": true,
  "files": [
    "runner.ts",
    "schema.ts",
    "deno.json",
    "README.md"
  ],
  "scripts": {
    "build": "echo \"no build step — runner.ts ships as source\"",
    "lint": "echo \"no lint — Deno typechecks at run time\"",
    "typecheck": "echo \"no typecheck — Deno typechecks at run time\"",
    "test": "deno test --config deno.json schema.test.ts"
  }
}
